<?php

/* operadores de comparacion 
 * = asignar
 * == igual
 * === identico
 * != diferente
 * !== no identico
 * < menor que
 * > mayor que
 * <= menor igual que
 * >= mayor igual que
 * */

$color = 'blue';

if ($color == 'red') {
   echo "The color is red";
} else { 
    echo "The color is not red";
}
